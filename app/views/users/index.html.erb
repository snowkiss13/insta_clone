<% provide(:title, 'All users') %>
<h1>All users</h1>

<ul class="users">
  <% @user.each do |user| %>
    <li>
      <%#= gravafor user, size: 50 %>
      <%= link_to user.name, user %>
    </li>
  <% end %>
</ul>