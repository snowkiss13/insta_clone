<p id="notice"><%= notice %></p>

<!--<h1>Feeds</h1>-->

<table>
  <thead>
<!--    <tr>-->
<!--      <th>Image</th>-->
<!--      <th colspan="3"></th>-->
<!--    </tr>-->
  </thead>

  <tbody>
      <% @feeds.each do |feed| %>
        <table align="center">
            <tr><%= feed.user.name %></tr>
            <tr><%= image_tag feed.image %></tr>
            <tr><%= feed.content %></tr>
            <tr><%= link_to 'Show', feed %></tr>
            <tr><%= link_to 'Edit', edit_feed_path(feed) %></tr>
            <tr><%= link_to 'Destroy', feed, method: :delete, data: { confirm: 'Are you sure?' } %></tr>
        </table>
      <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Feed', new_feed_path %>
